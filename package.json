{
  "name": "planeage",
  "version": "0.1.0",
  "description": "Resolve Flight # -> Tail # -> FAA mfr year -> aircraft age.",
  "main": "server.js",
  "scripts": {
    "refresh": "node scripts/refresh-faa.js",
    "start": "node server.js",
    "test": "node --test",
    "verify": "node --expose-gc scripts/verify.js",
    "verify:full": "node --expose-gc scripts/verify.js --full-scan",
    "deploy:gcp": "cloud/deploy.sh"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "type": "commonjs",
  "dependencies": {
    "@google-cloud/storage": "^7.18.0",
    "adm-zip": "^0.5.16",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "express-rate-limit": "^8.2.1",
    "express-validator": "^7.3.1",
    "helmet": "^8.1.0"
  }
}
